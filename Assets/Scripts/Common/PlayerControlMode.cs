/// <summary>
/// Determines whether a player is controlled by the user, AI, or by another user via multiplayer.
/// </summary>
public enum PlayerControlMode
{
    User = 0,
    AI = 1,
    Multiplayer = 2
}